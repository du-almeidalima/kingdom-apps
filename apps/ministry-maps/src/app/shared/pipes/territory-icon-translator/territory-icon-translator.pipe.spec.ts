import { TerritoryIconTranslatorPipe } from './territory-icon-translator.pipe';

describe('TerritoryIconTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new TerritoryIconTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
