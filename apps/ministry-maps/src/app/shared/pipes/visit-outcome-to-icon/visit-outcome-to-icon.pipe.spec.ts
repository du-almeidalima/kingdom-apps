import { VisitOutcomeToIconPipe } from './visit-outcome-to-icon.pipe';

describe('VisitOutcomeToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new VisitOutcomeToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
