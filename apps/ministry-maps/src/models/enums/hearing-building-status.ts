/**
 * Enum with the possible statuses of a {@link HearingBuilding}.
 * Similar to the {@link DesignationStatusEnum}, but this one refers to the building itself
 * and not the designation.
 */
export enum HearingBuildingStatusEnum {
  PENDING = 'PENDING',
  DONE = 'DONE',
}
